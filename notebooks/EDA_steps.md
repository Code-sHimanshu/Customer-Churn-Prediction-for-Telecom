EDA notebook steps:
1. Load and clean data (use src/data.py functions).
2. Print class balance: df['Churn'].value_counts(normalize=True)
3. Histograms for tenure, MonthlyCharges, TotalCharges.
4. Boxplots of MonthlyCharges grouped by Churn.
5. Bar chart: churn rate by Contract.
6. Heatmap of numeric correlations.
7. Save important observations to markdown cells.
